<div align="right">

**简体中文** · [English](README.md)

</div>

# AI标题生成

一个替你给笔记起名的思源插件。选中一篇或多篇笔记，插件向任意 OpenAI 兼容的 chat completions 接口请求标题，然后直接写回，或者先交给你确认。

## 为什么做这个

思源把新建文档命名为「无标题」。之后重命名是件手工体力活，而给一整个文件夹的笔记逐个改名繁琐到多数人干脆放弃。

这个插件把这道工序自动化：读取每篇笔记的正文，向模型请求标题，再通过内核自己的重命名链路写回——文档路径、子文档路径和搜索索引都保持同步。

## 功能

- **批量生成。** 在文档树中选中多篇笔记，一次完成。笔记会自动切分批次，批次之间并发执行。
- **写回前先确认。** 每个标题都可编辑：取消不想要的，改动任意一条，或单独重新生成一行。
- **提示词和风格自己定。** 系统提示词和用户提示词都可以改写，`{{content}}`、`{{language}}`、`{{style}}` 每次请求时填充。
- **兼容任意接口。** 可对接任何 OpenAI 兼容的 chat completions 服务，也可直接导入思源里已配置好的供应商。
- **可关闭思考。** 内置通义千问、vLLM、豆包等服务的请求体预设，另可自定义 JSON。
- **为排查而生。** 调试模式会把完整请求与响应打进 console。

## 环境要求

- 思源 3.8.3 或更高版本。
- 一个 OpenAI 兼容的 chat completions 接口，及其 Base URL 与 API Key。

## 安装

1. 从思源集市搜索`ai-title-siyuan`安装本插件
2. 或从 [最新发行版](https://github.com/wmy2981/ai-title-siyuan/releases/latest) 下载 `package.zip`，然后在思源中打开 **设置 → 集市 → 已下载**，点击 **从安装包安装**，选择该 zip，之后启用插件。

自行构建：

```bash
git clone https://github.com/wmy2981/ai-title-siyuan.git
cd ai-title-siyuan
npm ci
npm run build
```

产物 `package.zip` 会出现在仓库根目录。

## 配置

打开 **设置 → 集市 → 已下载 → AI标题生成 → 设置**。

1. **Base URL** —— 按你所用供应商的文档带上版本段
2. **API Key** —— 通过 `Authorization: Bearer <key>` 发送；接口无需密钥时留空。
3. **模型名称** —— 可直接输入，点击 **获取模型列表** 从供应商端点选择，或用 **从思源设置获取** 复制你已配置好的供应商。
4. 点击 **测试连接**。只要有回复就说明配置可用。

### 设置项说明

| 分组 | 设置项 | 默认值 | 说明 |
| --- | --- | --- | --- |
| 接口 | 协议 | Chat Completions API | 当前仅实现该协议。 |
| 接口 | 温度 | `1.0` | 取值 `0`–`2`，越低越稳定。 |
| 接口 | Top P | `0.8` | 留空则不发送该参数。 |
| 接口 | Top K | *空* | 留空则不发送。OpenAI 官方接口对未识别参数直接返回 400，仅在确认供应商支持时才填写。 |
| 接口 | 最大输出 token | `512` | 以 `max_completion_tokens` 发送；若供应商不认这个字段名，回退为 `max_tokens`。 |
| 行为 | 超时 | `10000` ms | 针对单次请求。批次是并发的，所以这不是共享预算。 |
| 行为 | 超时重试 | `1` | 仅在限流、服务端错误、超时和网络故障时重试。 |
| 行为 | 传入单个笔记内容长度 | `1000` 字符 | 每篇各自截断，保留开头部分。 |
| 行为 | 单次请求最多传入笔记数 | `3` | 超出会自动切分批次。 |
| 行为 | 最大并发请求数 | `3` | 遇到限流时调小。 |
| 行为 | 自动应用标题 | 禁用 | 见下文。 |
| 界面 | 显示顶栏按钮 | 开 | |
| 界面 | 显示面包屑按钮 | 关 | |
| 界面 | 显示文档树右键菜单 | 开 | |
| 界面 | 调试模式 | 关 | 把完整对话 JSON 输出到 console。 |

### 标题的应用方式

| 档位 | 单篇笔记 | 多篇笔记 |
| --- | --- | --- |
| **禁用**（默认） | 弹确认窗口 | 弹确认窗口 |
| **仅单篇笔记** | 直接写回 | 弹确认窗口 |
| **总是** | 直接写回 | 直接写回 |

批量改名是破坏性操作，而思源的重命名没有撤销栈。**批次请求失败时仍会先弹确认窗口**——那种情况下有一部分笔记没拿到标题，值得先看一眼再决定要不要应用其余的。

### 关闭思考

推理模型可能很慢，有些还会把全部输出放进推理字段而让 `content` 留空。**禁用思考** 开关会在请求体里带上 `reasoning_effort: "none"`。

用这个字段是因为它是**协议自带的写法，不是厂商扩展**：`reasoning_effort` 是 OpenAI 官方 Chat Completions 的参数，DeepSeek、Ollama、Gemini 2.5 系、GLM、qwen3.8-max、OpenRouter 都把它解释为「不推理」。

它不是万能钥匙，两种情况关不掉：

- 一贯思考的模型没有关闭档位（Gemini 2.5 Pro、Gemini 3 系、仅思考模式的千问等）；
- 严格的接口对不认识的参数直接返回 400，OpenAI 官方自己就是如此，GPT-6 Astra 连 `none` 都拒绝。

两种情况你都会收到提示。**开关开着、模型却仍然产生了推理**时（响应里带 `reasoning_content`，或 `usage` 里报出推理 token 数），生成结束时会提示「禁用思考未生效」。

这一点值得单独说：这类失败本身是**静默**的——请求照样成功，只是每次都在为一个用不上的思维链多付 token 和等待，光看结果是发现不了的。

## 使用

四个入口，行为一致：

- **顶栏按钮**：为当前正在编辑的笔记生成标题。
- 编辑器内的 **面包屑按钮**：同上。
- **在文档树中右键笔记**：为一篇或多篇生成标题。批量走这里。
- **命令面板**中的 *用 AI 生成标题*。

## 说明与限制

- **正文通过 `/api/export/exportMdContent` 获取**，而不是从编辑器缓冲读取。导出得到的是干净 Markdown，不会像 kramdown 接口那样在每个块后附加属性行。
- **内容为空的笔记会被跳过**，不发请求，避免模型为空文档凭空编造标题。
- **模型没有返回标题的笔记会被记为失败**。插件绝不猜测对应关系，也不会按顺序顶替，因为那会把标题写到毫不相干的笔记上。
- **`content` 为空时会回退读取 `reasoning_content`**，部分模型会把完整答案放在那里。
- **不支持发布服务。** 发布访客持有的是只读角色，而插件使用的内核端点要求管理员角色，因此 `disabledInPublish` 已设为 `true`。

## 排查

在设置中开启 **调试模式** 并打开 console，它会打印完整请求体、原始响应和每一步流程决策。

| 现象 | 多半是 |
| --- | --- |
| `HTTP 404` | Base URL 写错。对照供应商文档检查版本段。 |
| `HTTP 401` | API Key 错误或缺失。 |
| 报错里出现 `max_completion_tokens` | 你的供应商只认 `max_tokens`。插件会自动改用正确字段名重试；若你看到两次，说明重试也失败了。 |
| `HTTP 400` 且提到未知参数 | 供应商不认 `reasoning_effort`。关掉 **禁用思考**。 |
| 模型没有返回任何内容 | 推理可能耗尽了输出预算。调大 **最大输出 token** 或关闭思考。 |
| 标题不是我想要的语言 | 修改 **标题语言**，它默认跟随你的思源界面语言。 |

## 开发

```bash
npm run dev        # 改动时重新构建
npm run build      # 生产构建 + package.zip
npm run typecheck  # tsc --noEmit
npm run icon       # 重新生成 assets/icon.png 和 assets/preview.png
npm run preview    # 仅按 assets/preview.html 重拍 assets/preview.png
```

`npm run icon` 和 `npm run preview` 用 Playwright 拍摄预览图，首次使用前先执行 `npm install`，再执行 `npx playwright install chromium`。其余脚本都不依赖它。

## 许可

MIT，详见 [LICENSE](LICENSE)。
